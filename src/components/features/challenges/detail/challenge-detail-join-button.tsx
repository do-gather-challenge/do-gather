'use client';

import { Button } from '@/components/ui/button';

const ChallengeDetailJoinButton = () => {
  return (
    <Button variant="secondary" className="w-full">
      신청하기
    </Button>
  );
};

export default ChallengeDetailJoinButton;
